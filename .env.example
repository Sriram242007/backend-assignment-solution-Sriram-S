# Backend API
PORT=8080

# Persistent database
DB_PATH=backend_jobs.db

# Supplied processor
PROCESSOR_URL=http://127.0.0.1:8001/v1/process

# Worker configuration
WORKER_CONCURRENCY=10
WORKER_POLL_SECONDS=0.10
CLAIM_LEASE_SECONDS=15

# HTTP timeout for processor requests
HTTP_TIMEOUT_SECONDS=125